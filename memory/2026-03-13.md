# Memory Log — 2026-03-13

---

## 🔐 Security Review: PR #29 — Payment Multi-Provider Support (Raciq-BE)

**Status:** MERGED (post-merge review completed)

**Summary:**
Reviewed PR #29 adding Midtrans + Xendit multi-provider payment support to Raciq backend.

**Security Strengths:**
- Webhook signature verification (SHA512 + callback token) using `ActiveSupport::SecurityUtils.secure_compare`
- Database row locking (`payment.with_lock`) for race condition prevention
- API-first pattern (external API called before DB write)
- State machine protection with `ALLOWED_TRANSITIONS`
- Proper secret management via `.env.example` and `.gitignore`
- Token cache TTL reduced (20h → 50min) for Everpro integration

**Post-Merge Security Concerns:**
| Issue | Severity | Action |
|-------|----------|--------|
| Test bypass `status: "paid"` still active | 🔴 CRITICAL | Remove before production |
| Xendit nil check missing in `verify_signature` | 🟡 MEDIUM | Add fail-closed handling |
| No webhook rate limiting | 🟡 MEDIUM | Add Rack::Attack |

**Recommended Fixes:**
```ruby
# Xendit fail-closed fix:
def self.verify_signature(headers)
  callback_token = ENV["XENDIT_WEBHOOK_TOKEN"]
  return false if callback_token.blank?  # Fail closed
  # ... compare logic
end
```

---

## 📊 Daily Error Report Activity

**Cron Issue:** Error report cron was firing every 10 minutes instead of once daily at 6 AM. This appears to be a misconfigured cron schedule (`*/10 * * * *` instead of `0 6 * * *`).

**Recommendation:** Fix cron schedule to run once daily at 6 AM WIB.

---

## 🐛 New Error Detected (13 March 2026)

**Error:** `Socket::ResolutionError` — Cannot resolve SMTP hostname
**Service:** Hermina API Production (hermina-mobile-api)
**Timestamp:** 2026-03-13 16:54:19 WIB
**Context:** User registration attempt (mundi.mmn@gmail.com)
**Root Cause:** Infrastructure/DNS issue with SMTP server

**Pattern:** This is a recurring infrastructure issue affecting both web and mobile API services.

---

*Logged at 17:05 WIB*
